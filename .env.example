# =============================================================================
# D&D AN - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control - it's already in .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# ANTHROPIC CLAUDE API (REQUIRED)
# -----------------------------------------------------------------------------
# Used for:
# - AI Dungeon Master narratives
# - LangChain NPC agent conversations
# - Dynamic quest and dialogue generation
#
# Get your API key from: https://console.anthropic.com/
# The key should start with: sk-ant-api03-
#
ANTHROPIC_API_KEY=your_claude_api_key_here
VITE_ANTHROPIC_API_KEY=your_claude_api_key_here

# -----------------------------------------------------------------------------
# IMAGE GENERATION - AI Scene Generation
# -----------------------------------------------------------------------------
# Choose your image generation provider
# Options: 'placeholder' (free, instant), 'openai', 'replicate', 'stability'
VITE_IMAGE_PROVIDER=placeholder

# --- OpenAI DALL-E 3 ---
# Get your API key from: https://platform.openai.com/api-keys
# Best for: Quick setup, good quality, higher cost (~$0.04/image)
OPENAI_API_KEY=your_openai_api_key_here
VITE_OPENAI_API_KEY=your_openai_api_key_here

# --- Replicate ---
# Get your API key from: https://replicate.com/account/api-tokens
# Best for: Pixel art models, flexible model selection, pay-per-use (~$0.01/image)
REPLICATE_API_KEY=your_replicate_api_key_here
VITE_REPLICATE_API_KEY=your_replicate_api_key_here

# --- Stability AI ---
# Get your API key from: https://platform.stability.ai/
# Best for: Direct Stable Diffusion access, fine control, good pricing (~$0.02/image)
STABILITY_API_KEY=your_stability_api_key_here
VITE_STABILITY_API_KEY=your_stability_api_key_here

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------
# Backend server port (default: 3001)
# PORT=3001

# Node environment (development, production)
# NODE_ENV=development

# -----------------------------------------------------------------------------
# LANGCHAIN NPC CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------
# NPC update interval in milliseconds (default: 10000 = 10 seconds)
# Lower values = more responsive NPCs but more API calls
# VITE_NPC_UPDATE_INTERVAL=10000

# Maximum NPC interactions to store in memory (default: 50)
# VITE_NPC_MEMORY_LIMIT=50

# NPC agent temperature (0.0-1.0, default: 0.9)
# Higher = more creative/varied responses
# VITE_NPC_TEMPERATURE=0.9

# -----------------------------------------------------------------------------
# USAGE NOTES
# -----------------------------------------------------------------------------
# 1. Copy this file to .env
# 2. Set VITE_IMAGE_PROVIDER to your chosen provider:
#    - 'placeholder': Free, instant, for testing (1x1 pixel images)
#    - 'openai': DALL-E 3, easy setup, great quality
#    - 'replicate': Best for pixel art, flexible models
#    - 'stability': Stable Diffusion XL, good balance
# 3. Add the corresponding API key for your chosen provider
# 4. Restart dev server after changing environment variables
#
# For development: Use 'placeholder' (no API key needed)
# For production: Use 'openai' or 'replicate' for best results

# -----------------------------------------------------------------------------
# SECURITY NOTES
# -----------------------------------------------------------------------------
# 1. NEVER commit your .env file to version control
# 2. Keep your API keys secret
# 3. Use different keys for development and production
# 4. Rotate keys regularly
# 5. For production, use environment variables from your hosting platform
# 6. The VITE_ prefix exposes variables to frontend - only use for non-sensitive config
